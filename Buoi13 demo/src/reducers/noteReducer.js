import { CardActions } from "@material-ui/core";

const initState={
    id:''
}


function noteReducer(state=initState
    switch(action.type){
        default:
            return state;
    }
    
    )
export default initState;